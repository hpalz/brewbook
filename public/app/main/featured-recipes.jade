.panel.panel-primary
  .panel-heading.text-center Recently Brewed
  .panel-body
    .row(ng-repeat="recipe in recipes | filter:{featured:true}")
      .col-md-12
        a(href="/recipes/{{recipe._id}}") {{recipe.name}}
