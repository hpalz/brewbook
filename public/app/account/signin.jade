.container(ng-controller="mvNavBarLoginCtrl")
  .well
    form.form-horizontal(name="signinForm",fxlayoutalign='space-evenly stretch')
      fieldset
        legend Sign In
        center
          h6 Start by logging in or creating an account to begin your homebrew recipe collection
        .form-group
          .col-md-6
            input.form-control(placeholder="Email", ng-model="username")
        .form-group
          .col-md-6
            input.form-control(type="password", placeholder="Password", ng-model="password")
        .form-group
          .pull-right
            a(href="/signup") No Account? Sign up 
            | 
            button.btn.btn-primary(ng-click="signin(username, password)") Sign In